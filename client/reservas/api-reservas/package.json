{
  "name": "parse-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "parse-server --appId $PARSE_SERVER_APPLICATION_ID --masterKey $PARSE_SERVER_MASTER_KEY --databaseUri $PARSE_SERVER_DATABASE_URI"
  },
  "dependencies": {
    "parse-server": "file:"
  }
}
